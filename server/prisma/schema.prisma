generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Ticket {
  orderID String @id
  category String
  isUsed Boolean @default(false)
  qrToken String 
}